# Example Files

Use these files to test the web app manually.

Uploads:
- `paper.md`
- `library.bib`
- `figures.zip`

The zip contains `result_plots/example.png`, which matches the image path in
`paper.md`.
